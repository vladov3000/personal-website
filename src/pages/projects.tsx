import React from "react";
import { WebpageTemplate } from "../components/WebpageTemplate";

const Projects: React.FC<null> = () => {
  return (
    <WebpageTemplate>
      <p> This is the projects page </p>
    </WebpageTemplate>
  );
};

export default Projects;
